```
rocker/tidyverse
    --> rockerextra/python
        --> rockerextra/py-remote
            --> rockerextra/spark
                --> rockerextra/spark-master
                --> rockerextra/spark-worker
```

# Load pyenv automatically by adding
# the following to ~/.bash_profile:

export PATH="/root/.pyenv/bin:$PATH"
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"

# Load pyenv automatically by adding
# the following to your profile:

export PATH="/home/rstudio/.pyenv/bin:$PATH"
eval "$(pyenv init -)"
eval "$(pyenv virtualenv-init -)"

 . ./.bash_profile

# https://github.com/pyenv/pyenv/tree/master/plugins/python-build
env PYTHON_CONFIGURE_OPTS="--enable-optimizations" pyenv install 3.6.4 -v

PYTHON_CONFIGURE_OPTS="--enable-unicode=ucs4" pyenv install 2.7.4


http://damnwidget.github.io/anaconda/docker/